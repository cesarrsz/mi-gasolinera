<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Panel de Control - Siba</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = { theme: { extend: { colors: { sibaGreen: '#00604A', sibaRed: '#E0301E' } } } }
    </script>
</head>
<body class="bg-gray-100 pb-20">
    <nav class="bg-sibaGreen text-white p-4 shadow-lg sticky top-0 z-50">
        <div class="max-w-5xl mx-auto flex justify-between items-center">
            <h1 class="font-black text-xl">⚙️ Siba Admin</h1>
            <div class="flex gap-4">
                <a href="/" target="_blank" class="hover:text-gray-200">Ver Web</a>
                <a href="/admin/logout" class="bg-red-600 px-3 py-1 rounded text-sm hover:bg-red-700">Salir</a>
            </div>
        </div>
    </nav>

    <div class="max-w-5xl mx-auto mt-8 px-4">
        <form action="/admin/update" method="POST" class="space-y-6">
            
            <div class="bg-white p-6 rounded-xl shadow-lg">
                <h3 class="text-lg font-bold text-gray-800 mb-4 border-b pb-2">⛽ Precios de Combustible</h3>
                <div class="grid md:grid-cols-3 gap-4">
                    <div class="text-center">
                        <label class="block text-green-700 font-bold mb-1">Magna</label>
                        <input type="number" step="0.01" name="magna" value="<%= data.prices.magna %>" class="w-full text-3xl font-black text-center border-2 border-green-100 rounded p-2 focus:border-green-500 outline-none">
                    </div>
                    <div class="text-center">
                        <label class="block text-red-700 font-bold mb-1">Premium</label>
                        <input type="number" step="0.01" name="premium" value="<%= data.prices.premium %>" class="w-full text-3xl font-black text-center border-2 border-red-100 rounded p-2 focus:border-red-500 outline-none">
                    </div>
                    <div class="text-center">
                        <label class="block text-gray-700 font-bold mb-1">Diésel</label>
                        <input type="number" step="0.01" name="diesel" value="<%= data.prices.diesel %>" class="w-full text-3xl font-black text-center border-2 border-gray-200 rounded p-2 focus:border-gray-500 outline-none">
                    </div>
                </div>
            </div>

            <div class="bg-white p-6 rounded-xl shadow-lg">
                <h3 class="text-lg font-bold text-gray-800 mb-4 border-b pb-2">ℹ️ Información General</h3>
                <div class="grid md:grid-cols-2 gap-4">
                    <div class="col-span-2">
                        <label class="block text-sm text-gray-600 font-bold">Eslogan</label>
                        <input type="text" name="slogan" value="<%= data.slogan %>" class="w-full p-2 border rounded">
                    </div>
                    <div>
                        <label class="block text-sm text-gray-600 font-bold">Teléfono</label>
                        <input type="text" name="phone" value="<%= data.phone %>" class="w-full p-2 border rounded">
                    </div>
                    <div>
                        <label class="block text-sm text-gray-600 font-bold">Correo Contacto</label>
                        <input type="email" name="email" value="<%= data.email %>" class="w-full p-2 border rounded">
                    </div>
                </div>
            </div>

            <div class="bg-white p-6 rounded-xl shadow-lg">
                <h3 class="text-lg font-bold text-gray-800 mb-4 border-b pb-2">🕒 Horarios</h3>
                <div class="mb-4">
                    <label class="block text-sm text-gray-600 font-bold">Horario Bombas</label>
                    <input type="text" name="pump_hours" value="<%= data.pump_hours %>" class="w-full p-2 border rounded bg-yellow-50">
                </div>
                <div class="grid md:grid-cols-3 gap-4">
                    <div>
                        <label class="block text-xs text-gray-500 uppercase">Oficina Mañana</label>
                        <input type="text" name="office_morning" value="<%= data.office_morning %>" class="w-full p-2 border rounded">
                    </div>
                    <div>
                        <label class="block text-xs text-gray-500 uppercase">Oficina Tarde</label>
                        <input type="text" name="office_afternoon" value="<%= data.office_afternoon %>" class="w-full p-2 border rounded">
                    </div>
                    <div>
                        <label class="block text-xs text-gray-500 uppercase">Hora Comida</label>
                        <input type="text" name="office_lunch" value="<%= data.office_lunch %>" class="w-full p-2 border rounded">
                    </div>
                </div>
            </div>

            <button type="submit" class="w-full bg-sibaRed hover:bg-red-700 text-white font-black text-xl py-4 rounded-xl shadow-xl transition transform hover:-translate-y-1">
                💾 GUARDAR CAMBIOS
            </button>
        </form>
    </div>
</body>
</html>