<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Acceso Administrativo - Siba</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        sibaGreen: '#00604A',
                        sibaRed: '#E0301E',
                    }
                }
            }
        }
    </script>
</head>
<body class="bg-sibaGreen h-screen flex items-center justify-center px-4">

    <div class="bg-white p-8 rounded-2xl shadow-2xl max-w-sm w-full border-t-8 border-sibaRed">
        <div class="text-center mb-8">
            <h1 class="text-3xl font-black text-sibaGreen mb-2">Siba Admin</h1>
            <p class="text-gray-500 text-sm">Panel de Control de Precios</p>
        </div>

        <% if (error) { %>
            <div class="bg-red-100 border-l-4 border-sibaRed text-red-700 p-4 mb-6 text-sm" role="alert">
                <p class="font-bold">Error</p>
                <p><%= error %></p>
            </div>
        <% } %>

        <form action="/admin/login" method="POST" class="space-y-6">
            <div>
                <label class="block text-gray-700 text-sm font-bold mb-2">Usuario</label>
                <input type="text" name="username" required autofocus
                    class="w-full px-4 py-3 rounded-lg bg-gray-50 border border-gray-300 focus:border-sibaGreen focus:ring-2 focus:ring-sibaGreen/20 outline-none transition">
            </div>

            <div>
                <label class="block text-gray-700 text-sm font-bold mb-2">Contraseña</label>
                <input type="password" name="password" required
                    class="w-full px-4 py-3 rounded-lg bg-gray-50 border border-gray-300 focus:border-sibaGreen focus:ring-2 focus:ring-sibaGreen/20 outline-none transition">
            </div>

            <button type="submit" 
                class="w-full bg-sibaRed hover:bg-red-700 text-white font-bold py-3 rounded-lg shadow-lg transform transition hover:-translate-y-0.5">
                Entrar al Sistema
            </button>
        </form>
    </div>

    <div class="absolute bottom-4 text-white/50 text-xs">
        &copy; 2025 Siba Combustibles
    </div>
</body>
</html>