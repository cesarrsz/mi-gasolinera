<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Acceso Admin - Siba</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = { theme: { extend: { colors: { sibaGreen: '#00604A', sibaRed: '#E0301E' } } } }
    </script>
</head>
<body class="bg-sibaGreen h-screen flex items-center justify-center">
    <div class="bg-white p-8 rounded-xl shadow-2xl w-96 border-t-8 border-sibaRed">
        <h2 class="text-2xl font-black text-center text-sibaGreen mb-6">Siba Admin</h2>
        <% if (error) { %>
            <div class="bg-red-100 text-red-700 p-3 rounded mb-4 text-sm"><%= error %></div>
        <% } %>
        <form action="/admin/login" method="POST" class="space-y-4">
            <div>
                <label class="block text-sm font-bold text-gray-700">Usuario</label>
                <input type="text" name="username" class="w-full p-2 border rounded focus:ring-2 focus:ring-sibaGreen outline-none">
            </div>
            <div>
                <label class="block text-sm font-bold text-gray-700">Contraseña</label>
                <input type="password" name="password" class="w-full p-2 border rounded focus:ring-2 focus:ring-sibaGreen outline-none">
            </div>
            <button type="submit" class="w-full bg-sibaRed text-white font-bold py-2 rounded hover:bg-red-700 transition">ENTRAR</button>
        </form>
    </div>
</body>
</html>